{
  // If you need jsconfig.json for JavaScript tooling, keep only non-TypeScript options here.
  "compilerOptions": {
    // Enable experimentalDecorators for Angular projects to support @Component, @Injectable, etc.
    "experimentalDecorators": true,
    "moduleResolution": "node",
    "ignoreDeprecations": "6.0"
  },
  "exclude": [
    "dist"
  ],
  "angularCompilerOptions": {
    "strictTemplates": true
  }
}